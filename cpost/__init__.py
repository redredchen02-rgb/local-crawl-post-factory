"""cpost — local-first crawl-to-post automation toolkit (namespaced package root)."""

# Distribution name: the PyPI dist-name differs from the import-name (cpost ≠ local-crawl-post-factory).
# Centralised here so _ctx.py and tests reference one authoritative string (D1).
__dist_name__ = "local-crawl-post-factory"
