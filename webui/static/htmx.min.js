/* Placeholder. Vendor the real HTMX here for the UI interactions to work:
 *   make vendor-htmx
 * or:
 *   curl -L https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js -o webui/static/htmx.min.js
 * The pages render without it; only the live polling/partial-swap needs it. */
